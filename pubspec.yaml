name: docwellness
description: 'DocWellness - one-on-one dietician guidance with meal logging, adaptive diet plans, and progress tracking.'
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+4

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  dio: ^5.9.0
  get: ^4.7.2
  shared_preferences: ^2.5.3
  flutter_secure_storage: ^9.2.4
  buttons_tabbar: ^1.3.15
  image_picker: ^1.2.1
  fl_chart: ^1.1.1
  google_fonts: ^6.3.2
  lottie: ^3.3.2
  intl: ^0.20.2
  socket_io_client: ^3.0.2
  cached_network_image: ^3.4.1
  youtube_player_flutter: ^9.1.3
  share_plus: ^11.0.0
  path_provider: ^2.1.5
  image_gallery_saver_plus: ^4.0.1
  permission_handler: ^11.3.1
  record: ^6.2.0
  audioplayers: ^6.6.0
  video_player: ^2.10.0
  webview_flutter: ^4.10.0
  url_launcher: ^6.3.1
  # 10.x no longer compiles on this Flutter SDK (it subclasses IconData,
  # which the framework now declares `final`). 11.0.0 has an open, unresolved
  # bug with icons going missing on clean/CI *release* web builds
  # (fluttercommunity/font_awesome_flutter issue #301), but that's a release-
  # build issue, not a debug/dev-server one, and there's no newer version to
  # pin to instead - watch that issue before shipping a web release build.
  font_awesome_flutter: ^11.0.0
  sentry_flutter: ^8.14.2
  posthog_flutter: ^5.31.0
  connectivity_plus: ^7.3.0
  # Push notifications (Goal Journey Timeline nudges) - versions matched to
  # docwellness-dietician's pubspec for consistency across both apps.
  firebase_core: ^4.7.0
  firebase_messaging: ^16.2.0
  flutter_local_notifications: ^21.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  flutter_native_splash: ^2.4.6
  flutter_launcher_icons: ^0.14.3

# App launcher icon (home screen icon) - mark only, no wordmark/tagline
# baked in, since the OS already shows the app's display name (see
# android:label in AndroidManifest.xml) underneath the icon on the home
# screen - text inside the icon itself would be redundant and, once
# adaptive-icon-masked, likely clipped anyway. adaptive_icon_foreground is
# already safe-zone-padded (see icon_adaptive_foreground.png's generation)
# so the ring isn't clipped by the circle/squircle/rounded-square mask
# different launchers apply. Regenerate with:
#   dart run flutter_launcher_icons
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icons/icon_mark.png"
  adaptive_icon_background: "#FEF6FB"
  adaptive_icon_foreground: "assets/icons/icon_adaptive_foreground.png"

# Native (Android/iOS) launch screen shown before the Flutter engine draws
# its first frame - fills the "wrong icon flashes first" gap the app's
# default unconfigured launcher icon otherwise leaves. Uses the mark-only
# icon (not the full logo+wordmark+tagline lockup) because this native splash
# gets circle/squircle-masked by the OS (especially android_12's
# windowSplashScreenAnimatedIcon) - text would get clipped. The SAME
# background color here matches lib/app/modules/splash/views/splash_view.dart
# (the Dart-drawn splash shown immediately after, which DOES show the full
# logo+wordmark+tagline), so the two feel like one continuous screen instead
# of a jarring double-splash. Regenerate the native assets after changing
# this with:
#   dart run flutter_native_splash:create
flutter_native_splash:
  color: "FEF6FB"
  image: assets/icons/icon_adaptive_foreground.png
  android_12:
    color: "FEF6FB"
    image: assets/icons/icon_adaptive_foreground.png

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/icons/
    - assets/images/
    - assets/demo_image/
    - assets/levels/
    - assets/videos/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:

  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
